{
  "CN": "kubernetes",
  "hosts": [
    "127.0.0.1",
    "{{ MASTER_IP }}",
    "{{ inventory_hostname }}",
{% for HOST in MASTER_CERT_HOSTS %}
    "{{ HOST }}",
{% endfor %}
    {% for master in groups['kube-master'] %}
        "{{ master }}",
    {%  endfor %}
    "{{ CLUSTER_KUBERNETES_SVC_IP }}",
    "kubernetes",
    "kubernetes.default",
    "kubernetes.default.svc",
    "kubernetes.default.svc.cluster",
    "kubernetes.default.svc.cluster.local"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "ChengDu",
      "L": "XS",
      "O": "k8s",
      "OU": "System"
    }
  ]
}
